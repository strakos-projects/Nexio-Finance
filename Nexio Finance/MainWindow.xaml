<Window x:Class="NexioFinance.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:Nexio_Finance.Properties" 
        Title="{x:Static properties:Resources.AppTitle}" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen" Background="#F3F4F6">

    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#374151"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#1F2937">
            <StackPanel>
                <TextBlock Text="{x:Static properties:Resources.AppHeading}"
                           Foreground="White" FontSize="28" FontWeight="Bold" Margin="30,40,30,40"/>

                <RadioButton Content="{x:Static properties:Resources.MenuDashboard}" Style="{StaticResource MenuButtonStyle}" IsChecked="True" Checked="Menu_Dashboard_Checked"/>
                <RadioButton Content="{x:Static properties:Resources.MenuTransactions}" Style="{StaticResource MenuButtonStyle}" Checked="Menu_Transactions_Checked"/>
                <RadioButton Content="{x:Static properties:Resources.MenuAnalysis}" Style="{StaticResource MenuButtonStyle}" Checked="Menu_Analysis_Checked"/>
                <RadioButton Content="{x:Static properties:Resources.MenuSettings}" Style="{StaticResource MenuButtonStyle}" Checked="Menu_Settings_Checked"/>
            </StackPanel>
        </Border>

        <ContentControl x:Name="MainContentArea" Grid.Column="1" Margin="0"/>

    </Grid>
</Window>